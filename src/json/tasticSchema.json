{
    "id": "https://frontastic.cloud/json/tasticSchema",
    "title": "Tastic Schema",
    "description": "A Frontastic tastic definition",
    "type": "object",
    "definitions": {
        "fieldDefinition": {
            "allOf": [
                { "$ref": "https://frontastic.cloud/json/schema/common#/definitions/fieldDefinition" },
                { "$ref": "#/definitions/tasticFieldDefinitionAdditions" }
            ]
        },
        "tasticFieldDefinitionAdditions": {
            "type": "object",
            "properties": {
                "type": {
                    "anyOf": [
                        { "$ref": "https://frontastic.cloud/json/schema/common#/definitions/fieldType" },
                        {
                            "type": "string",
                            "enum": [
                                "custom",
                                "group"
                            ]
                        }
                    ]
                }
            }
        }
    },
    "properties": {
        "tasticType": {
            "type": "string"
        },
        "name": {
            "type": "string"
        },
        "description": {
            "type": "string"
        },
        "schema": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "name": {
                        "type": "string"
                    },
                    "fields": {
                        "type": "array",
                        "items": {
                            "$ref": "#/definitions/fieldDefinition"
                        }
                    }
                },
                "required": ["name", "fields"]
            }
        }
    },
    "required": ["tasticType", "name", "schema"]
}
